It is an application that you can use to exchange currencies (data is received from www.NBP.pl). To exchange currency simple send GET request with JSON to .../exchange Example:

{
"fromCurrency":"USD",
"toCurrency":"EUR",
"value":"100"
}
